import styled from 'styled-components';

export default styled.button`
    height: 8vh;
    margin-bottom: 25px;
    font-size; 1rem;
    font-weight: bold;
    background: purple;

    &:hover {
        cursor: pointer;
        color: white;
    }
`;